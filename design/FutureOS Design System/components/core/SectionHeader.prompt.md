Labels a group of setting rows; sits above a `Card`, never inside one.

```jsx
<SectionHeader>תצוגה</SectionHeader>
```

One or two Hebrew words, no colon, no uppercase — the emphasis comes from 700 weight at 13sp and 1sp letter-spacing at 55% opacity, not from case.
