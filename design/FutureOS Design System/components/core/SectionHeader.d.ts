/**
 * SettingHeader — a one-word group label above a Card.
 */
export interface SectionHeaderProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export function SectionHeader(props: SectionHeaderProps): JSX.Element;
