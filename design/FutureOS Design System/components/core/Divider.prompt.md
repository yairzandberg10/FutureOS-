Separates rows inside a `Card`; the only line in the system.

```jsx
<Divider />
```

Never between list items on the screen background (those are separated by `itemSpacing`, not a line), and never around a card.
