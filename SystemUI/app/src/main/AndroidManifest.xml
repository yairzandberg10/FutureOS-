<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_WALLPAPER_INTERNAL" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <!-- ממשק מערכת: אפליקציות אחרונות, התראות (אייקון/שם של כל אפליקציה) ופתיחת
         אפליקציות. בלי זה Package Visibility מסתיר את רוב החבילות, ו-Recents
         הראה רק את מי ש"דיבר" איתנו קודם. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <!-- לצורך צ'יפ "שיחה פעילה" בשורת המצב (StatusBarScreen, TelecomManager.isInCall) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- לתזמון האזעקות המדויקות של DndScheduleManager - הרשאה "רגילה" (לא runtime) ב-API 31 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- כדי לחדש את תזמון ה-DND אחרי הפעלה מחדש (AlarmManager מתאפס באתחול) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <uses-permission android:name="com.future.futureui.permission.SYSTEM_SETTINGS" />
    <uses-permission android:name="com.future.keyboard.permission.SETTINGS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:appCategory="productivity"
        android:isGame="false"
        android:theme="@style/Theme.SystemUI">

        <provider
            android:name=".theme.ThemeProvider"
            android:authorities="com.android.sistemui.theme"
            android:exported="true" />

        <provider
            android:name=".provider.SystemUiSettingsProvider"
            android:authorities="com.android.sistemui.provider"
            android:exported="true" />

        <service
            android:name=".statusbar.service.StatusBarAccessibilityService"
            android:exported="true"
            android:label="SystemUI Status Bar"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".controlcenter.service.ControlCenterAccessibilityService"
            android:exported="true"
            android:label="SystemUI Control Center"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".notificationcenter.service.NotificationCenterAccessibilityService"
            android:exported="true"
            android:label="SystemUI Notification Center"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".notificationcenter.service.HeadsUpNotificationService"
            android:exported="false" />

        <receiver
            android:name=".dnd.DndScheduleReceiver"
            android:exported="false" />

        <receiver
            android:name=".dnd.DndBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".controlcenter.service.MediaControlService"
            android:label="SystemUI Media Controller"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="התאמה אישית"
            android:theme="@style/Theme.SystemUI" />

        <!-- בכוונה בלי CATEGORY_LAUNCHER: SystemUI הוא SystemUI, לא אפליקציה שהמשתמש
             פותח ישירות - אין לו אייקון ברשימת האפליקציות. ACTION_MAIN נשאר כדי
             שאפשר עדיין להפעיל אותו במפורש (adb / debugging) בלי חשיפה בלאנצ'ר. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SystemUI"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>