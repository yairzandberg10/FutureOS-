# Contact (אנשי קשר)

`com.future.contact` — Contacts.

Real `ContactsContract` query (no mock data), list + detail view with call/message actions (`ACTION_DIAL`/`ACTION_SENDTO`). "Add contact" delegates to the system contact editor via `ACTION_INSERT` rather than reimplementing a raw-contact editor.
