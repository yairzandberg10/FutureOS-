# Calendar (לוח שנה)

`com.future.calendar` — Hebrew calendar.

Built around `HebrewDateFormatter`/`HebrewNumerals` for real Hebrew-calendar dates, `DafYomi` for the daily Talmud page, and `LocationHelper`/`CalendarSettings` for location-based scheduling (zmanim). `CalendarRepository` backs the data layer.
